# Demo (live mode) and eval scripts. Copy to .env or export before running.
# Inception key — get one at https://platform.inceptionlabs.ai
# The demo reads MERCURY_API_KEY; the eval scripts accept either name.
MERCURY_API_KEY=
INCEPTION_API_KEY=
# Anthropic key — only needed for the eval baselines (Haiku, Opus).
ANTHROPIC_API_KEY=
